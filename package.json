{
  "name": "scaler-calendly-clone-root",
  "private": true,
  "scripts": {
    "build": "cd FRONTEND && rm -rf node_modules && npm install && npm run build && cd .. && rm -rf public && mv FRONTEND/dist public",
    "install-backend": "cd BACKEND && npm install"
  }
}
